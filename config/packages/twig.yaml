twig:
    paths: ['%kernel.project_dir%/templates']
    debug: '%kernel.debug%'
    strict_variables: '%kernel.debug%'
    globals:
        app_webmaster : Lionel GOUTOULLI (lionel.goutoulli@gmail.com)